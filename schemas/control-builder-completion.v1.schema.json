{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PRJ226 control builder completion v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "invocation_id",
    "task_id",
    "status",
    "summary",
    "blockers",
    "changed_paths",
    "provider_output_declaration"
  ],
  "properties": {
    "schema_version": {"const": "PRJ226.CONTROL_BUILDER_COMPLETION.v1"},
    "invocation_id": {"type": "string", "minLength": 1},
    "task_id": {"type": "string", "minLength": 1},
    "status": {"enum": ["COMPLETED", "BLOCKED"]},
    "summary": {"type": "string", "minLength": 1},
    "blockers": {"type": "array", "items": {"type": "string"}},
    "changed_paths": {"type": "array", "items": {"type": "string"}},
    "provider_output_declaration": {
      "type": ["string", "null"],
      "description": "Unverified provider-supplied output summary; the adapter does not recompute it."
    }
  }
}
